<h2>Details for {{ table_name }} - ID: {{ row['id'] }}</h2>
<table border="1">
    <tbody>
        {% for column in columns %}
            <tr>
                <th>{{ column }}</th>
                <td>{{ row[column] }}</td>
            </tr>
        {% endfor %}
    </tbody>
</table>
